# local scratch
*.zip
verify.py
shot*.py
*.png
!assets/**/*.png

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Figure API credentials — the repo is public, this must never be committed
api/config.php
api/figure-errors.log
